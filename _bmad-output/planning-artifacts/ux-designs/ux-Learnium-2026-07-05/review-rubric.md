# UX Spine Review — Rubric Coverage & Consistency

**Reviewer lens:** rubric coverage & consistency · **Date:** 2026-07-05 · **Targets:** `DESIGN.md`, `EXPERIENCE.md` (status: draft)

**Verdict:** Conditional pass — both spines are structurally complete, sections in canonical order, all four key flows have named protagonists + explicit climaxes, and PRD FR-1→FR-23 are all represented; the one thing to fix before handoff is cross-reference integrity — **7 `{components.*}` tokens referenced in EXPERIENCE.md do not resolve to any DESIGN.md frontmatter key.**

---

## Findings

| Severity | Location | Issue | Suggested fix |
|---|---|---|---|
| **major** | EXPERIENCE.md L29, L90, L95, L96, L109, L113, L132 → DESIGN.md frontmatter `components:` (L115–153) | **Cross-reference integrity break.** 7 component tokens referenced in EXPERIENCE.md have **no matching key** in DESIGN.md frontmatter (they exist only as prose bullets in DESIGN.md's Components markdown section). Unresolved: `{components.nav-top-bar}` (L29), `{components.nav-mobile-bottom-bar}` (L29), `{components.lesson-node}` (L90), `{components.leaderboard-row}` (L95), `{components.review-session-card}` (L96), `{components.toast}` (L109 & L132), `{components.celebration-overlay}` (L113). The spine's own closing line (L218) claims "every value referenced above uses `DESIGN.md` tokens by name" — currently false for these. Known/flagged in `.memlog.md` L24. | Add the 7 keys to DESIGN.md `components:` frontmatter (kebab-case, mirroring the existing prose specs), each with its token-referenced values. Resolved tokens that already match: `xp-pill`, `streak-flame`, `nova-avatar`, `button-progress`. |
| **major** | DESIGN.md L117–120 (`button-primary`) | **Flagship CTA has no dark-mode fill token.** Frontmatter says `background: '{colors.brand}'  # dark: '{colors.background-dark}' inverts to white fill` — but `background-dark` is `#000000` (black), not white. The intended dark inverted CTA (white fill + black text, per prose L173) cannot be driven from any existing token; `foreground: '{colors.on-brand}'` (white) is also correct only for light mode, not the dark black-text case. Contradicts both the "drive every value from tokens" Do-rule (L240) and "co-equal dark" (L166). | Add explicit dark tokens (e.g. a white surface token + reference `on-brand-dark` `#000000` for text), or a `-dark` variant of `button-primary`, so the inverted CTA resolves cleanly in both modes. Fix the misleading comment. |
| **minor** | DESIGN.md L224 (Components prose) / frontmatter L115–153 | `level-badge` (listed suspect) is documented as prose ("Level badge / ring") but is **not** a frontmatter key. It happens not to be referenced as a `{components.*}` token in EXPERIENCE.md, so it is not a *broken* reference — but the frontmatter is incomplete vs the documented component set. Same applies to prose-only `input`, `category-pill`, `progress-ring`. | Add `level-badge` (and optionally input/category-pill/progress-ring) to frontmatter for a complete, resolver-clean component set. |
| **minor** | EXPERIENCE.md L43–61 (surfaces table) vs L65 (FR map) | **FR-19 share cards** and **FR-20 friends leaderboard** are covered only by a passing mention in the FR→surface map (L65); neither gets its own row in the "All surfaces" table. Friends leaderboard is folded into the Leagues row (L57) whose stated purpose omits it; share-card generation has no surface. | Add explicit surface rows (or sub-surface notes) for share-card generation and the friends leaderboard so FR-19/FR-20 have a concrete surface, not just a map line. |
| **minor** | EXPERIENCE.md L44–61 vs L54–55, L113 | **FR-18 Path-completion celebration** is a first-class celebration in the PRD (Badge + shareable summary + next-Path suggestion) but the surfaces table lists only "Set-complete celebration" and "Level-up celebration"; Path-complete is captured only inside the Success/celebration state row (L113). | Add a "Path-complete celebration" surface row, or note it explicitly alongside Set-complete, to fully trace FR-18. |
| **minor** | DESIGN.md frontmatter `typography.numeral` (L82–85) | `numeral` role defines `fontFamily`/`fontWeight`/`fontVariantNumeric` but no `fontSize`/`lineHeight`. It is referenced widely (XP, streak, level, %) as if a standalone role. Intentional (size inherited from context), but worth a one-line note so consumers don't treat it as fully specified. | Add a `note` clarifying numeral inherits size from its host role, or leave as-is if the resolver handles inheritance. |

---

## Rubric scorecard

**1. DESIGN.md completeness — PASS.** All 8 canonical body sections present and in spec order: Brand & Style (L156) → Colors (L164) → Typography (L180) → Layout & Spacing (L190) → Elevation & Depth (L196) → Shapes (L202) → Components (L212) → Do's/Don'ts (L236). Frontmatter tokens valid: `name`, `description`, `colors` (full light+dark kebab-case set), `typography` (3-role + numeral), `rounded` (xl/2xl/full), `spacing` (4pt scale + content-max), `components` (9 keys). Only gap: frontmatter component set is a subset of the prose-documented components (see findings).

**2. EXPERIENCE.md completeness — PASS.** All 8 required sections present: Foundation (L17), IA (L27), Voice & Tone (L67), Component Patterns (L84), State Patterns (L99), Interaction Primitives (L115), Accessibility Floor (L127), Key Flows (L140). Three invented sections each justified inline: Retention Loop ("invented — launch-critical", L185), Quota/Tiers/Paywall ("invented", L197), Content Integrity ("invented — anti-hallucination", L206). All four key flows carry a named protagonist and an explicit **CLIMAX** beat: UJ-1 Dana (L152), UJ-2 Marcus (L162), UJ-3 Priya (L172), UJ-4 Ravi (L182).

**3. Cross-reference integrity — FAIL (see major finding #1).** 7 unresolved `{components.*}` tokens. All `{colors.*}`, `{typography.*}`, `{rounded.*}`, `{spacing.*}` references in EXPERIENCE.md resolve. Assumption cross-refs (`#3,#4,#5,#6,#11,#12,#13`) and open-question refs (`§9 Q1–Q5`) all map correctly to PRD §10 / §9.

**4. PRD coverage — PASS.** Every FR area (FR-1→FR-23) is represented by a surface via the IA surfaces table (L43–61) and the explicit FR→surface map (L65). Minor tracing weaknesses on FR-18/FR-19/FR-20 noted above (surfaces exist in the map but not as dedicated table rows).

**5. Internal consistency — PASS (with the two token-level defects above).** No contradictions between the spines: domain split is clean (DESIGN = visual, EXPERIENCE = behavior), pricing placeholder matches (Free 5 gen + 30 chats; both cite `[ASSUMPTION #11]`), 5-tab nav matches DESIGN's "4–5 icon tabs", streak-amber / lime-reward reservations consistent across both, and the audit-fix commitments (real controls, toasts-not-alert, history-safe nav) align with the memlog. The only internal defects are the `button-primary` dark-fill token gap and the missing component frontmatter keys.
